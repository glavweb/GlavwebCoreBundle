{% set fields %}
    {%- for field, translation in fields %}

    label_{{ field }} : {{ translation }}
    {%- endfor %}
{% endset %}

filter:
    {{- fields }}

list:
    {{- fields }}

form:
    {{- fields }}

show:
    {{- fields }}

dashboard:
    label_{{ normalizedServiceId }}: Unknown

breadcrumb:
    link_{{ normalizedServiceId }}_list: Unknown
